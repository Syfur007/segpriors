# Auto-generated by scripts/gen_iccit_configs.py — do not hand-edit.
# F2 capacity control — RGB (m1) width-matched to m4's param count.
# target_params=27,984 achieved_params=27,384 relative_error=2.14% channels=[4, 8, 16, 24, 32]
# Record this match in the run manifest at training time (orchestration.manifest.RunManifest.record) — this comment is generation-time documentation only, not re-verified at run time.
compose:
- ../../base.yaml
- ../../dataset/isic18.yaml
- ../../model/mkunet/t.yaml
model:
  in_channels: 3
  channels:
  - 4
  - 8
  - 16
  - 24
  - 32
dataset:
  channel_mode: m1
  batch_size: 16
logging:
  experiment_name: iccit_mkunet_m4_matched_isic18
