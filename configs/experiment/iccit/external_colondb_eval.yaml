# Auto-generated by scripts/gen_iccit_configs.py — do not hand-edit.
# C4 — ColonDB as a strictly external cohort, evaluated once via eval.py --allow-test-eval (get_standard_loaders()/get_fold_loaders() raise ExternalDatasetError; get_test_loader() works normally).
# In practice: compose this dataset/external override on top of whichever iccit training config produced the checkpoint being evaluated, so channel_mode/in_channels match that checkpoint — the m1 default here is only for standalone sanity checks.
compose:
- ../../base.yaml
- ../../dataset/colondb.yaml
- ../../model/mkunet/t.yaml
model:
  in_channels: 3
dataset:
  channel_mode: m1
  external: true
logging:
  experiment_name: iccit_external_colondb_eval
